# Отчет о проделанной работе: Прошивка для STM32F4 🚀

## 📋 Описание проекта
Разработана прошивка для микроконтроллера **STM32F4**, которая выводит сообщение `"Hello, Nikita"` в терминал через UART.

## 🛠️ Выполненные этапы работы

### 1. 🏗️ Установка и настройка инструментов
- ✅ Установлена библиотека **PlatformIO**
- ✅ Настроена среда разработки для работы с STM32F4
- ✅ Создан проект с соответствующей конфигурацией

### 2. 💻 Разработка кода
Создан файл `main.c` со следующим содержанием:

```c
#include <stdio.h>
#include "stm32f4xx.h"

int main(void) {
    // Инициализация периферии
    SystemInit();
    
    // Настройка UART для вывода в терминал
    // (конкретная реализация зависит от используемых пинов)
    
    while(1) {
        printf("Hello, Nikita\n");
        // Задержка для удобства чтения вывода
        for(int i = 0; i < 1000000; i++);
    }
    
    return 0;
}
```

### 3. ⚙️ Конфигурация проекта
Файл `platformio.ini` содержит настройки для целевой платформы:
```ini
[env:stm32f4]
platform = stm32
board = genericSTM32F407VET6
framework = cmsis
```

## 🧪 Тестирование в Renode 1.16.0

### 🔧 Выполненные команды:

| № | Команда | Описание |
|---|---------|-----------|
| 1 | `mach create` | 🏭 Создание виртуального микрочипа |
| 2 | `machine LoadPlatformDescription @platforms/boards/stm32f4_discovery-kit.repl` | 📁 Загрузка готовой конфигурации из папки Renode |
| 3 | `sysbus LoadELF ".../firmware.elf"` | 📤 Загрузка скомпилированной прошивки |
| 4 | `showAnalyzer sysbus.usart2` | 📟 Активация анализатора для USART2 |
| 5 | `s` (или `start`) | ▶️ Запуск эмуляции |

### 📊 Результаты тестирования
- ✅ Прошивка успешно загружена в эмулятор
- ✅ Сообщение `"Hello, Nikita"` корректно выводится в терминал
- ✅ Система стабильно работает в эмулируемой среде

## 🎯 Итоги
- ✔️ Прошивка успешно разработана и протестирована
- ✔️ Использованы современные инструменты разработки
- ✔️ Реализована функциональность вывода в UART
- ✔️ Проведено тестирование в эмуляторе Renode

## 📁 Структура проекта
```
project/
├── src/
│   └── main.c
├── platformio.ini
└── README.md
```

**Статус проекта:** ✅ Завершен успешно! 🎉

<img width="382" height="295" alt="Снимок экрана 2025-11-21 134009" src="https://github.com/user-attachments/assets/7abde961-0375-496c-9a0b-20e51b42febe" />

